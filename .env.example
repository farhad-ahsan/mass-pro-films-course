# Server-side YouTube Data API v3 key, used only by api/playlist.js.
# This must NEVER be prefixed with VITE_ (that would expose it to the browser).
# Set the real value in Vercel: `vercel env add YOUTUBE_API_KEY`
YOUTUBE_API_KEY=your-new-restricted-youtube-api-key
